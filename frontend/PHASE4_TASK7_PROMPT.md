Task: Phase 4 — TipTap UX polish

Implement the following in the React frontend using TipTap:
1) Floating/inline formatting toolbar (appears near selection)
2) Slash commands menu (e.g., /heading, /bullet, /numbered, /code, /quote, /divider, /image)
3) Drag-to-reorder blocks (paragraphs, headings, lists)

Requirements:
- Keep TypeScript strict and build clean
- Accessible keyboard navigation for menus
- Persist editor content to backend (existing API) on save
- Add minimal tests where feasible

Process:
- npm ci; npm run dev for local testing; npm run build to verify
- Commit and PUSH after each meaningful chunk

When completely finished, run: openclaw system event --text "Done: Phase 4 — TipTap toolbar, slash commands, drag-to-reorder implemented" --mode now
