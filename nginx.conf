upstream backend {
    server backend:8000;
}

upstream frontend_next {
    server frontend-next:3000;
}

server {
    listen 80;
    server_name localhost;
    client_max_body_size 10M;

    # API and admin requests → Django backend
    location /api/ {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /admin/ {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Django static files
    location /static/ {
        alias /app/staticfiles/;
        expires 7d;
        add_header Cache-Control "public, max-age=604800, immutable";
    }

    # User-uploaded media
    location /media/ {
        alias /app/media/;
        add_header Cache-Control "public, max-age=300";
    }

    # Everything else → Next.js app; HTML no-store
    location / {
        proxy_pass http://frontend_next;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        add_header Cache-Control "no-store";
    }
}

# Rollback note:
# To rollback to Svelte frontend, restore previous upstream and / location to frontend-svelte:5173
